asociar una función handler a la señal SIGINT
handler prende una bandera que señala la finalización del programa.

Verificar si existe el archivo para reestart
    si existe
        inicializa todo con los datos proporcionados e inicia el ciclo de iteraciones
        y no toma los datos desde matriz.dat
    sino
        crea un nuevo archivo y empieza desde 0 con las iteraciones, e inicializa la información desde matriz.dat.

dentro del while se hace una verificación de la bandera, cada vez que se termine el ciclo de iteraciones.
    si la bandera está encendida guarda los datos actuales en un nuevo archivo.

datos actuales por guardar:
    iteración
    productos
    dimensiones
    matriz a
    dimenisones
    matriz B
